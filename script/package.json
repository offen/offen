{
  "name": "script",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "budo index.js --live --ssl --cert local.offen.dev.pem --key local.offen.dev-key.pem",
    "test": "mochify --allow-chrome-as-root ./**/*.test.js",
    "posttest": "standard",
    "fix": "standard --fix",
    "prebuild": "mkdir -p dist",
    "build": "browserify index.js -p tinyify > dist/script.js"
  },
  "dependencies": {
    "offen": "file:./../packages"
  },
  "devDependencies": {
    "browserify": "^16.2.3",
    "budo": "^11.6.2",
    "envify": "^4.1.0",
    "mochify": "^6.2.0",
    "standard": "^12.0.1",
    "tinyify": "^2.5.0"
  },
  "browserify": {
    "transform": [
      "envify"
    ]
  },
  "standard": {
    "env": [
      "mocha"
    ]
  }
}
