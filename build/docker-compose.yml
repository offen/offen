version: '3'

services:
  proxy:
    build:
      context: './..'
      dockerfile: build/proxy/Dockerfile
    ports:
      - 3000:80
    depends_on:
      - server

  server:
    build:
      context: './..'
      dockerfile: build/server/Dockerfile
