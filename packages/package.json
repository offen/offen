{
  "name": "offen",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "mochify --allow-chrome-as-root ./**/*.test.js",
    "posttest": "standard",
    "fix": "standard --fix"
  },
  "dependencies": {},
  "devDependencies": {
    "mochify": "^6.2.0",
    "standard": "^12.0.1"
  },
  "standard": {
    "env": [
      "mocha"
    ]
  }
}
